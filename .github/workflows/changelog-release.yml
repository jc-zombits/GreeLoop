name: Changelog & Release

on:
  push:
    tags:
      - 'v*.*.*'

permissions:
  contents: write

jobs:
  changelog_release:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Generate Changelog and Release
        uses: TriPSs/conventional-changelog-action@v5
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          tag-prefix: 'v'
          output-file: 'CHANGELOG.md'
          release-count: 0
          preset: 'conventionalcommits'
          fail-on-error: true
          # Evitar cambios en versión/commit/tag cuando el disparo es por tag
          skip-version-file: true
          skip-bump: true
          skip-tag: true
          skip-commit: true
          # Publicar el Release basado en el tag recibido
          create-release: true